# Privacy

网页反馈标注器是一个本地优先的 Chrome Manifest V3 扩展。

## 数据保存位置

扩展会把以下数据保存到浏览器本地的 `chrome.storage.local`：

- 当前页面标题和 URL。
- 用户添加的反馈分类、反馈内容和反馈日期。
- 文本反馈中的选中文字。
- 区域反馈、标记点反馈和文本反馈对应的局部截图。
- 用户主动配置的 AI Provider、Base URL、Model 和 API Key。
- AI 检查生成的待确认结果。

## AI 检查的数据边界

AI 检查默认关闭。只有用户主动配置 AI 服务并点击检查时，扩展才会向用户选择的服务发送数据。

AI 检查会发送：

- 从当前页面抽取的可见正文文本块。

AI 检查不会发送：

- 页面截图。
- 人工反馈内容。
- 本地保存的截图证据。
- 账号信息或遥测数据。

API Key 保存在浏览器本地 `chrome.storage.local`。这不是加密密钥库，请不要在不可信设备上保存敏感 Key。

## 数据不会上传到项目维护者服务器

当前版本不包含以下能力：

- 不使用项目维护者的云服务器。
- 不做云同步。
- 不做账号系统。
- 不采集使用统计。
- 不包含第三方分析 SDK。

如果用户启用 AI 检查，文本会发送到用户自己配置的第三方或内部 AI 服务，该行为由用户主动触发。

## 权限说明

扩展声明以下权限：

- `activeTab`：在用户主动点击扩展或进入批注模式后读取当前标签页信息。
- `debugger`：仅在用户主动导出快照 HTML 时，通过 Chrome DevTools Protocol 截取当前标签页的整页画面。调试会话在导出成功、失败或页面关闭后立即断开；扩展不使用该权限读取网络请求、控制台日志或执行调试操作。
- `scripting`：向当前页面注入批注交互脚本。
- `storage`：把反馈数据和设置保存在浏览器本地。
- `notifications`：AI 检查完成或失败时发送系统通知。
- `file:///*`：仅在用户于 Chrome 扩展详情页主动开启“允许访问文件网址”后，用于本地 HTML 批注和导出。

扩展还声明 `optional_host_permissions`。只有当用户配置 AI 服务并保存设置时，才会请求访问对应服务域名。

## 导出文件

用户主动导出的 HTML 或 PDF 只在本机生成，内容来自本地保存的反馈数据、AI 待确认结果和截图证据。快照 HTML 的整页图片通过本机 Chrome CDP 生成，不发送到外部截图服务。导出后的文件由用户自行保存和分享。

## 删除数据

用户可以在扩展弹窗中清空当前页面反馈。删除后，对应页面保存在 `chrome.storage.local` 中的人工反馈和 AI 待确认结果会被移除。
