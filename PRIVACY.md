# Privacy

网页反馈标注器是一个本地优先的 Chrome Manifest V3 扩展。

## 数据保存位置

扩展会把以下数据保存到浏览器本地的 `chrome.storage.local`：

- 当前页面标题和 URL。
- 用户添加的反馈分类、反馈内容和反馈时间。
- 文本反馈中的选中文字。
- 区域反馈、标记点反馈和文本反馈对应的局部截图。

## 数据不会被上传

当前版本不包含以下能力：

- 不接入 AI API。
- 不上传服务器。
- 不做云同步。
- 不做账号系统。
- 不采集使用统计。
- 不包含第三方分析 SDK。

## 权限说明

扩展仅声明以下权限：

- `activeTab`：在用户主动点击扩展或进入批注模式后读取当前标签页信息。
- `scripting`：向当前页面注入批注交互脚本。
- `storage`：把反馈数据保存在浏览器本地。

## 导出文件

用户主动导出的 PDF 只在本机生成，内容来自本地保存的反馈数据和截图。导出后的文件由用户自行保存和分享。

## 删除数据

用户可以在扩展弹窗中清空当前页面反馈。删除后，对应页面保存在 `chrome.storage.local` 中的反馈数据会被移除。

