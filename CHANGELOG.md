# Changelog

## 0.5.15 (local, not released)

- 快照 HTML 的整页截图改为 `chrome.debugger` 调用 CDP `Page.captureScreenshot`，不再依赖页面滚动和 `captureVisibleTab` 拼接。
- 普通长度页面使用单段文档坐标截图；超长页面使用连续、无重叠的文档坐标分片，避免滚动重排造成正文割裂。
- CDP 调试会话仅在用户主动导出快照期间连接，并在成功、失败或页面关闭后立即断开。
- 保留 `captureVisibleTab` 作为单条反馈局部证据截图，不扩大其他批注流程的权限使用范围。
- 新增 CDP 真实调用边界、连续分片与失败断开回归测试，并补充权限和错误处理说明。

## 0.5.14 (local, not released)

- 支持在用户开启 Chrome“允许访问文件网址”后批注本地 `file://` HTML，并补充明确的权限引导。
- 新增 Google Gemini 原生 Provider，使用 `generateContent` 与 `x-goog-api-key`，并自动迁移旧的原生 Gemini 自定义设置。
- AI 功能区新增常驻设置入口，切换 Provider 时不再静默复用旧服务的 API Key。
- 将 AI Provider 的预设、校验、请求构建和响应读取集中到 `src/ai-provider.js`。
- 快照 HTML 改为从页面顶部到页面底部的连续截图，不再按标记范围生成离散片段；原滚动拼接方案已在 0.5.15 被 CDP 截图替代。
- 截图请求统一串行节流，遵守 Chrome 每秒最多两次 `captureVisibleTab` 的限制。
- 新增页面访问、AI Provider 和连续快照三组回归测试与本地长页面测试夹具。

## 0.5.13

- 新增网页快照 HTML 报告导出能力，生成带截图快照、页面编号标记和右上角“修改点全览”的 HTML 文件。
- HTML 修改点全览参考信息流样式，以反馈内容为主，分类和时间作为辅助信息。
- 弹窗新增“导出快照 HTML”入口，页面内悬浮菜单的导出入口改为导出快照 HTML。
- 导出报告中的标注时间默认只展示日期，避免暴露精确操作时间。
- 新增“AI 检查页面”能力，支持用户配置自己的 AI 服务后生成待确认页面标记。
- PDF 导出支持人工反馈优先展示，并在文档末尾追加 AI 检查结果。
- AI 检查默认关闭，不内置 API Key，外部服务访问通过可选域名权限授权。
- AI 结果点击加入反馈后转为正式人工反馈，非问题结果会被删除并排除在导出外。
- 人工文本反馈优先使用贴近原文的下划线标记。
- AI 检查完成或失败时尝试系统通知，通知失败会记录中文原因用于排查。
- 优化 AI 低错结果质量，过滤错别字中的语义改写建议，并对 `属是 -> 属实` 这类确定性错词做本地修正。
- 优化 AI 返回格式容错，支持 JSON 对象、JSON 数组、Markdown JSON 代码块和“未发现问题”文本，并对格式异常自动重试。
- 新增当前需求基线、代码结构说明和需求反查记录，补充 AI 核心冒烟测试。

## 0.5.12

- 基于目标视觉稿继续优化 PDF 报告样式，固定报告标题并弱化页面来源信息。
- 提升标注点标题、元信息、反馈正文、引用文本和截图证据的层级关系。
- 减少报告中的装饰性容器，改用轻分隔线、编号圆点、左侧引用线和截图细边框。

## 0.5.11

- 优化 PDF 报告为信息流布局，减少大面积矩形卡片和重复分区，提高单页信息展示效率。
- 每条反馈改为“标注点编号 + 分类/类型/时间小字 + 反馈内容 + 截图证据”的顺序，优先传递实际反馈内容。
- PDF 报告不再展示反馈位置描述，避免无效元信息占用版面。

## 0.5.10

- 优化 PDF 反馈报告版式，增加更清晰的报告头部、摘要信息、反馈卡片和页脚。
- 优化每条反馈的信息层级，将反馈时间、反馈位置、反馈内容和证据截图分区展示。
- 优化竖向截图和不同宽高比截图的展示约束，减少报告中的视觉变形和拥挤感。

## 0.5.9

- 页面内铅笔悬浮入口会识别同侧已有的 `fixed` / `sticky` 悬浮组件并自动避让。
- 优化悬浮菜单定位，菜单打开时不再推动铅笔入口位置。
- 保持自动避让策略保守，避免误判普通页面内容。

## 0.5.8

- 支持拖动页面内铅笔悬浮入口。
- 悬浮入口松手后自动吸附到左右侧边。
- 悬浮入口在非使用状态下半隐藏，鼠标靠近或打开菜单时完整显示。
- 记住悬浮入口的侧边和垂直位置偏好。

## 0.5.7

- 增强页面内批注浮层的数据同步逻辑，`chrome.storage.local` 变化后自动刷新当前页标记和悬浮球数量。
- 新增发布前手动回归测试用例文档。

## 0.5.6

- 支持删除单条反馈。
- 删除后同步刷新插件弹窗反馈清单、当前页反馈数量和页面内编号标记。
- 删除后导出 PDF 不再包含已删除反馈。
- 清空当前页反馈后同步通知页面内批注浮层刷新。

## 0.5.5

- 优化反馈填写卡片在页面边缘和底部的弹出位置，避免保存按钮不可点击。
- 修复非文本反馈误显示引用文本的问题。
- 优化弹窗信息层级、帮助入口和当前页面反馈清单展示。
- 优化 PDF 截图展示，避免竖图、长图被拉伸变形。
- 保持数据本地保存，不接入 AI API、云同步或账号系统。
